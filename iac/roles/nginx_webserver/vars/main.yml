# --- Variables for the Nginx web server role in the IaC pipeline. These variables define the site title, environment name, database credentials, and API key for the service. ---
# --- Variables para el rol del servidor web Nginx en el pipeline de IaC. Estas variables definen el título del sitio, el nombre del entorno, las credenciales de la base de datos y la clave API para el servicio. ---
---
site_title: "Pipeline Demo App"
environment_name: "production"
db_user: "db_admin"
db_password: "DemoPassword123!"
api_key_servicio: "demo_api_key_987654321"